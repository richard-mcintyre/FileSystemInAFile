<Window x:Class="FileSystemInAFile.Browser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileSystemInAFile.Browser"
        mc:Ignorable="d"
        Title="Browser" Height="450" Width="800">

    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:MainWindow.ExitApplicationCommand}"
                        Executed="ExitApplicationCommand_Executed" />
        
    </Window.CommandBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition MinHeight="100" />
            <RowDefinition Height="5" /> <!-- Splitter row -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Command="{x:Static local:MainWindow.ExitApplicationCommand}" />
            </MenuItem>
        </Menu>

        <ListBox Grid.Row="1" x:Name="lstEntries"
                 ItemsSource="{Binding Entries}"
                 SelectedItem="{Binding SelectedItem}"
                 MouseDoubleClick="ListBox_MouseDoubleClick" />

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="Gray" ShowsPreview="True" />

        <Image Grid.Row="3" x:Name="img" Stretch="Uniform" />
    </Grid>
    
</Window>
